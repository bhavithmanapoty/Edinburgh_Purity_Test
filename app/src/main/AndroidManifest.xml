<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.edinpuritytest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EdinburghPurityTest">
        <activity android:name=".HomeScreen"></activity>
        <activity android:name=".Questions"></activity>
        <activity android:name=".ContactUs"></activity>
        <activity android:name=".ScoreScreen"></activity>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713 -->
        <!-- My AdMob ID: pub-6105353816616318 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="pub-6105353816616318"/>//Replace with own ID after testing and before publication

    </application>
</manifest>